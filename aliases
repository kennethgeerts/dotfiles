alias bi='bundle install'
alias bu='bundle update'

alias cat='bat'

alias d='kitty +kitten diff'

alias dev='hivemind Procfile.development'

alias gd='git difftool --no-symlinks --dir-diff'
alias gda='gd HEAD'

alias icat='kitty +kitten icat'

alias icloud='~/Library/Mobile\ Documents/com~apple~CloudDocs/'

alias ls='exa -al --color=always --group-directories-first' # my preferred listing
alias la='exa -a --color=always --group-directories-first'  # all files and dirs
alias ll='exa -l --color=always --group-directories-first'  # long format
alias lt='exa -aT --color=always --group-directories-first' # tree listing
alias l='ls'

alias lg='lazygit'

alias m='./bin/meta'

alias n='nnn -e'

alias ping='prettyping'

alias pushenvs='g push dev && g push uat && m dev d && m uat d'

alias roru='yarn upgrade -L && gem_update'

alias v='vim'

alias weer='curl -4 http://wttr.in/Merendree | less'

alias restart-pipeline='sudo service sidekiq restart && passenger-config restart-app'
